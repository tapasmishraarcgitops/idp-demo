---
apiVersion: devopstoolkitseries.com/v1alpha1
kind: ClusterClaim
metadata:
  name: idpdemocluster
  namespace: infra
spec:
  id: idpdemocluster
  compositionSelector:
    matchLabels:
      provider: civo
      cluster: ck-all
  parameters:
    nodeSize: small
    minNodeCount: 1
